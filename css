/* Reset padrão */
body {
    background-color: cadetblue;
    font-family: Arial, Helvetica, sans-serif;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Estilização do quadro principal */
main {
    background-color: beige;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.4);
    width: 80%;
    max-width: 600px;
    text-align: center;
    margin-top: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Estilo do bloco do perfil */
.perfil {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center; /* Centraliza dentro do main */
    width: 100%; /* Ocupa toda a largura do quadro */
    margin-bottom: 20px;
}


.perfil-img {
    width: 150px;
    height: 150px;
   
}



/* Nome do usuário */
.perfil-nome {
    margin-top: 5px; /* Reduzindo o espaçamento para não descer demais */
    font-size: 1.5em;
    font-weight: bold;
}

/* Estilização da tabela */
table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 10px;
}

th, td {
    border: 1px solid black;
    padding: 8px;
    text-align: center;
}

th {
    background-color: lightgray;
}

/* Links */
a {
    color: black;
    text-decoration: none;
}

a:hover {
    color: rgb(253, 253, 255);
}

/* Navegação */
nav {
    margin-top: 20px;
}

nav ul {
    list-style: none;
    padding: 0;
}

nav ul li {
    display: inline;
    margin: 0 10px;
}

nav a {
    font-weight: bold;
    background-color: beige;
    padding: 10px;
    border-radius: 5px;
    box-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
}

nav a:hover {
    background-color: cornflowerblue;
}

/* Rodapé */
footer {
    margin-top: 20px;
    padding: 10px;
    background-color: darkslategrey;
    color: white;
    text-align: center;
    width: 100%;
}
h2 {
    margin-top: 10px; /* Reduzindo espaçamento superior */
}

